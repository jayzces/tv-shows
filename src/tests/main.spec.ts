import { beforeEach, describe, expect, it, vi } from 'vitest'

describe('Main config file', () => {
  it('can mount app', () => {
    document.body.innerHTML = '<div id="app"></div>'
    // require('../main')

    // const nav = document.querySelector('.nav-header')
    // expect(nav).toBeTruthy()
  })
})
